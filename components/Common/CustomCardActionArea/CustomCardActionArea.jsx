import { CardActionArea } from '@mui/material';
import React from 'react';

const CustomCardActionArea = ({ children }) => {
  return <CardActionArea>{children}</CardActionArea>;
};

export default CustomCardActionArea;
